/**
 * Problema: Recebe ums string e duplica o seu conteudo
 * 
 * exemplo:
 * int nSize = strlen(string);
 * 
 * char stringDuplicate[nSize * 2];
 * 
 * //first part
 * strcpy(stringDuplicate, string);
 * 
 * for (int i = nSize; i < nSize; i++)
 * {
 *      stringDuplicate[i] = string[k];
 *      //next value for string
 *      k++;
 * }
 * 
 * or
 * 
 * char stringDuplicate[strlen(string)];
 * 
 * strcpy(stringDuplicate, string);
 * strcat(stringDuplicate, string);
*/

