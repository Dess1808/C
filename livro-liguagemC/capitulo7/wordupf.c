/**
 * Problema:
 * Recebe uma string, coloca a primeira letra de cada palavra em maiuscula e as restantes em minusculas. separacao
 * entre palavras e feita por espacos em brancos
 * 
 * hipotese:
 * deixar tudo em lower case para depois deixar a primeira letra em upper case, ainda sem ideias
 * 
 * hipotese 2
 * depois de cada espaco em branco deixar o proximo caracter em upper case
 * 
 * exemplo
 * //lower first
 * for (int i = 0; i < nSize; i++)
 * {
 *      string[i] = tolower(string[i]);
 * } 
 * 
 * //verificacao
 * 
*/