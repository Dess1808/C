#include<stdio.h>
#include<cs50.h>

//val = valor/Pot((1 + taxa), anos);
//pot - r = r * n //n times

int main(void)
{
    int anos = get_int("anos: ");
    float taxa = get_int("taxa: ");
    float  caixa = get_int("valor: ");
}