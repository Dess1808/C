#include<stdio.h>
#include<math.h>

int main(void)
{
    double a;
    double b;
    double c;

    scanf("%lf %lf %lf", &a, &b, &c);

    //delta
    double delta = pow(b, 2) - 4 * a * c;   

    //verificar impossibilidade de cauculos de raizes
    
    return 0;
}